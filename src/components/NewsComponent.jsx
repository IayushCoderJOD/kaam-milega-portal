import React from 'react'

const NewsComponent = () => {
    return (
        <div className='text-3xl flex flex-wrap items-center w-[50%]'>
            Up until now I am getting the api result and seeing it in the console now I need to update the news Component card to add the details and then show it on the page
        </div>
    )
}

export default NewsComponent